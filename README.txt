Error when installing twint with "pip3 install twint". Had to run the following afterwards to get it to install correctly
pip3 install --user --upgrade git+https://github.com/himanshudabas/twint.git@origin/twint-fixes#egg=twint

